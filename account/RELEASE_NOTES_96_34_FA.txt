ZIVO zivo60.96.34 — CLEANUP RELIABILITY

اصلاحات اصلی:
- فرمان «پاکسازی» بدون عدد اکنون پاکسازی امن پیش‌فرض 100 پیام را اجرا می‌کند.
- پاکسازی عددی قبل از عملیات پیام شروع فوری می‌دهد تا کاربر بداند فرمان دریافت شده.
- حذف batchهای پاکسازی تا 3 بار retry کنترل‌شده دارد.
- بعد از timeout، در صورت پشتیبانی گروه پیام‌های واقعاً باقی‌مانده verify می‌شوند و فقط همان‌ها retry می‌شوند.
- خطاهای Permission باعث retry storm نمی‌شوند.
- رفتار پاکسازی کامل گپ و حفاظت پیام‌های پین‌شده حفظ شده است.

QA:
check_zivo60_96_34_cleanup_reliability.py
