ZIVO zivo60.96.29

Behavioral QA hotfix — Forward Lock + Pet interaction

1) قفل فوروارد
- تشخیص ساختاری سریع قبلی حفظ شد.
- اگر حذف Live با verify تأیید نشود، دیگر لاگ «enforced» موفق ثبت نمی‌شود.
- حذف ناموفق/نامطمئن در SQLite به‌صورت pending-delete پایدار ثبت می‌شود.
- Worker مستقل پس از Retry دوباره نبود پیام را Verify می‌کند.
- نتیجه Retry در moderation audit ثبت می‌شود.
- مجازات کاربر از نتیجه حذف مستقل باقی مانده تا اخطار/مجازات از بین نرود.
- Event پس از برخورد consume می‌شود تا همان پیام وارد مسیرهای moderation دیگر و دوباره‌کاری نشود.

2) پت
- اسم ثبت‌شده پت اکنون یک Route واقعی دارد.
- نمونه ورودی‌های پشتیبانی‌شده: «رکس»، «رکس بیا»، «رکس بیا اینجا»، «رکس خوبی؟»، «رکس چطوری؟»، «رکس کجایی».
- Resolver به user_id وابسته است؛ پت کاربر دیگر با اسم مشابه برای فرستنده Route نمی‌شود.
- اسم پت از SQLite خوانده می‌شود و بعد از restart/reconfigure نیز قابل Resolve است.
- متن موفقیت اسم‌گذاری و Help اصلاح شد تا قابلیت واقعی صدا زدن پت را نشان دهد.

3) سازگاری
- Campaign queue/fairness نسخه 96.28 دست‌نخورده حفظ شده است.
- تمام Regressionهای موجود پروژه به‌علاوه تست رفتاری جدید اجرا شده‌اند.
